package com.example.eggventure.viewmodel.stepcounter

/**
 * Data class representing the step sensor data.
 *
 * Used mainly for testing purposes to encapsulate the total steps counted by the step sensor.
 */
data class StepSensorData(
    val totalSteps: Int
)
